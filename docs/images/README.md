# Images

- `interaction-flow.png` — state machine of a transfer, every path (source: `flow.dot`)
- `system-architecture.png` — hardware, firmware, serial protocol, dashboard (source: `arch.dot`)
- `dashboard-*.png` — screenshots of the live dashboard

Regenerate the diagrams with Graphviz: `dot -Tpng flow.dot -o interaction-flow.png`
